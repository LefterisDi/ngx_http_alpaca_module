alpaca_dist_html_size ./Probabilistic/prob.dist; # Path to the distribution file, relative to where the tester is initiated
                                                 # More specifically, the provided relative path "./Probabilistic/prob.dist"
                                                 # works when the tester is executed from inside the ALPaCA_Tester directory